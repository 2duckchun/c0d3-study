/* 
객체는 현실의 자동차와 같은 개념이다.
자동차는 색깔이 있고 무게가 있고 디자인이 있고 재료가 있다.
이처럼 객체는
색깔 : 파랑
무게 : 1톤
디자인 : 포터
처럼
키:값 형태로 존재한다.
키:값을 묶어놓은 것을 프로퍼티라고 한다.
키(프로퍼티 이름)는 문자열(또는 심볼)이 올 수 있으며
값(프로퍼티 값)에는 undefined외에 모든 값이 다 올 수 있다.
*/

// To create an object, you use the new keyword.
// Here's an example :

// create a Person Object
const a = new person();

// create an Array Object
const b = new Array();

const data = new Data();
const Func = new Function();
const Arr = new Array();
const Obj = new Object();
const Event = new EventEmitter();